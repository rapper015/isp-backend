"""Device Management Service — TR-069 CPE management control plane (Milestone 7).

Business-facing device-management platform that securely integrates with a
separately deployed GenieACS CWMP service. GenieACS owns protocol sessions,
the parameter tree, RPC execution and pending tasks; this service owns tenant
ownership, device business identity, model/capability catalogues, vendor-neutral
versioned profiles, configuration jobs with verification, controlled actions,
remote diagnostics, firmware approval/rollouts, drift, reconciliation and full
auditability."""
