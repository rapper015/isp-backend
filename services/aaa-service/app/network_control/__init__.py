"""Milestone 3 — Advanced Network Control (AAA bounded context).

Submodules are imported explicitly by callers; the package __init__ stays
minimal to avoid import cycles (policy -> network_control.radius_compiler)."""
